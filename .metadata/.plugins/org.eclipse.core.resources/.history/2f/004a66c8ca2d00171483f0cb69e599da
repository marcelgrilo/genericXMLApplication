package control.dao;

import javax.persistence.EntityManager;

public class ServerDao  extends GenericDao<ServerDao, Integer> {

    public ServerDao(EntityManager entityManager) {
        super(entityManager);
    }
    
}
